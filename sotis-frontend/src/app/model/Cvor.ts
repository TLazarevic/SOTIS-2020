export class Cvor {
    cvorId: number
    id: string
    label: string

    constructor(cvorId: number, id: string, label: string) {
        this.cvorId = cvorId
        this.id = id
        this.label = label
    }
}